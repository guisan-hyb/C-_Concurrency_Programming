## 浅谈单例模式的线程安全问题

```cpp
//利用智能指针解决释放问题
class SingleAuto
{
private:
    SingleAuto()
    {
    }
    SingleAuto(const SingleAuto&) = delete;
    SingleAuto& operator=(const SingleAuto&) = delete;
public:
    ~SingleAuto()
    {
        std::cout << "single auto delete success " << std::endl;
    }
    static std::shared_ptr<SingleAuto> GetInst()
    {
        // 1 处
        if (single != nullptr)
        {
            return single;
        }
        // 2 处
        s_mutex.lock();
        // 3 处
        if (single != nullptr)
        {
            s_mutex.unlock();
            return single;
        }
        // 4处
        single = std::shared_ptr<SingleAuto>(new SingleAuto);
        s_mutex.unlock();
        return single;
    }
private:
    static std::shared_ptr<SingleAuto> single;
    static std::mutex s_mutex;
};

//利用智能指针解决释放问题
class SingleMemoryModel
{
private:
    SingleMemoryModel()
    {
    }
    SingleMemoryModel(const SingleMemoryModel&) = delete;
    SingleMemoryModel& operator=(const SingleMemoryModel&) = delete;
public:
    ~SingleMemoryModel()
    {
        std::cout << "single auto delete success " << std::endl;
    }
    static std::shared_ptr<SingleMemoryModel> GetInst()
    {
        // 1 处
        if (_b_init.load(std::memory_order_acquire))
        {
            return single;
        }
        // 2 处
        s_mutex.lock();
        // 3 处
        if (_b_init.load(std::memory_order_relaxed))
        {
            s_mutex.unlock();
            return single;
        }
        // 4处
        single = std::shared_ptr<SingleMemoryModel>(new SingleMemoryModel);
        _b_init.store(true, std::memory_order_release);
        s_mutex.unlock();
        return single;
    }
private:
    static std::shared_ptr<SingleMemoryModel> single;
    static std::mutex s_mutex;
    static std::atomic<bool> _b_init ;
};

std::shared_ptr<SingleMemoryModel> SingleMemoryModel::single = nullptr;
std::mutex SingleMemoryModel::s_mutex;
std::atomic<bool> SingleMemoryModel::_b_init = false;


1 为对象allocate一块内存空间
2 调用construct构造对象
3 将构造到的对象地址返回

1 为对象allocate一块内存空间
2 先将开辟的空间地址返回
3 调用construct构造对象


为什么这种方式实现的单例有以上风险，而下面两种单例模式实现没有风险：
class SingletonOnce {
private:
    SingletonOnce() = default;
    SingletonOnce(const SingletonOnce&) = delete;
    SingletonOnce& operator = (const SingletonOnce& st) = delete;
    static std::shared_ptr<SingletonOnce> _instance;

public :
    static std::shared_ptr<SingletonOnce> GetInstance() {
        static std::once_flag s_flag;
        std::call_once(s_flag, [&]() {
            _instance = std::shared_ptr<SingletonOnce>(new SingletonOnce);
            });

        return _instance;
    }

    void PrintAddress() {
        std::cout << _instance.get() << std::endl;
    }

    ~SingletonOnce() {
        std::cout << "this is singleton destruct" << std::endl;
    }
};

std::shared_ptr<SingletonOnce> SingletonOnce::_instance = nullptr;


class Single2 {
private:
    Single2()
    {
    }
    Single2(const Single2&) = delete;
    Single2& operator=(const Single2&) = delete;
public:
    static Single2& GetInst()
    {
        static Single2 single;
        return single;
    }
};
```



这是一个非常经典的C++并发编程问题，涉及**双重检查锁定模式（DCLP）的缺陷**以及**指令重排**带来的风险。

你提到的两种内存分配和构造的顺序：
*   **顺序1**：分配内存 -> 调用构造函数 -> 返回地址 （**安全**）
*   **顺序2**：分配内存 -> 返回地址 -> 调用构造函数 （**危险，即指令重排后的结果**）

下面详细解释为什么前两种实现（`SingleAuto` 和 `SingleMemoryModel`）会面临顺序2的风险，而后两种（`SingletonOnce` 和 `Single2`）没有。

---

### 一、 为什么 `SingleAuto` (普通的DCLP) 有重排风险？

在 `SingleAuto::GetInst()` 中，关键代码是：
```cpp
single = std::shared_ptr<SingleAuto>(new SingleAuto);
```
这行代码实际上包含了三个步骤：
1. **分配内存**：`operator new` 开辟一块内存空间。
2. **构造对象**：调用 `SingleAuto` 的构造函数在这块内存上初始化对象。
3. **赋值发布**：将构造好的对象地址赋值给 `single`（即发布给其他线程可见）。

**风险来源：编译器和CPU的指令重排**

在C++中，构造函数的调用和将指针赋值给变量之间并没有严格的先后依赖关系（在单线程看来，先构造还是先赋值都没区别）。因此，编译器或CPU为了优化性能，可能会将指令重排成：
1. **分配内存**
2. **赋值发布**（先把未初始化的地址赋给 `single`）
3. **构造对象**（再慢慢调用构造函数）

**灾难场景：**
1. **线程A** 进入 `GetInst`，加锁，执行到 `new SingleAuto`。由于指令重排，内存分配后，**先将地址赋给了 `single`，此时 `single` 已经不是 nullptr 了，但对象还没构造！**
2. **线程B** 此时调用 `GetInst`，执行到外层的 `if (single != nullptr)`。
3. 因为线程A已经赋值了地址，**线程B判断条件为真，直接返回了 `single`！**
4. **线程B** 拿着这个未构造完成的“半成品”指针去调用成员函数，**发生未定义行为（崩溃或数据错乱）**。

---

### 二、 为什么 `SingleMemoryModel` 引入原子变量依然有风险？

`SingleMemoryModel` 试图用 `std::atomic<bool> _b_init` 配合 `memory_order` 来解决重排问题。它的逻辑是：先赋值 `single`，再通过 `release` 语义设置 `_b_init = true`，读取时通过 `acquire` 语义保证看到 `single` 的正确值。

**但是，这段代码依然是有隐患的：**
在C++标准中，**对 `std::shared_ptr` 的普通赋值操作（`single = ...`）并不是原子操作**。
即使你用 `_b_init` 做了同步标志，但 `shared_ptr` 内部的控制块（引用计数等）和裸指针的赋值是非常复杂的。如果在赋值 `single` 的过程中（`shared_ptr` 还没完全接管指针），另一个线程读取了 `single`（哪怕 `_b_init` 为 true），依然可能读到一个内部状态损坏的 `shared_ptr`。

C++20 引入了 `std::atomic<std::shared_ptr>` 就是为了解决这个问题。在 C++20 之前，DCLP 配合 `shared_ptr` 从根本上就是不可靠的。

---

### 三、 为什么 `SingletonOnce` 没有风险？

```cpp
static std::shared_ptr<SingletonOnce> GetInstance() {
    static std::once_flag s_flag;
    std::call_once(s_flag, [&]() {
        _instance = std::shared_ptr<SingletonOnce>(new SingletonOnce);
    });
    return _instance;
}
```

**安全原因：`std::call_once` 提供了强有力的线程同步和内存序保证。**

1. **内部阻塞机制**：如果线程A正在执行 `call_once` 内部的 Lambda，其他线程B、C等调用 `call_once` 时，**会被阻塞等待**，而不是像 DCLP 那样直接通过 `if` 判断就返回。
2. **Happens-Before 保证**：C++标准保证，当线程A的 `call_once` 执行完毕并返回后，线程B进入 `call_once` 时，**线程A在 Lambda 内执行的所有内存写入（包括 `new SingletonOnce` 的构造和 `shared_ptr` 的赋值），对线程B绝对可见**。
3. **不存在重排穿透**：因为其他线程根本不会在初始化期间拿到半成品指针，它们会被挡在 `call_once` 之外，直到对象100%构造完成并成功赋值给 `_instance`，其他线程才会放行并返回 `_instance`。

---

### 四、 为什么 `Single2` ( Meyers' Singleton ) 没有风险？

```cpp
static Single2& GetInst() {
    static Single2 single;
    return single;
}
```

**安全原因：C++11 标准对局部静态变量的线程安全性做了强制规定。**

C++11 标准（§6.7 [stmt.dcl]）明确规定：
> "如果控制流进入声明变量的语句时，变量正在被其他线程初始化，则当前线程将阻塞等待，直到初始化完成。"

这就是著名的 **"Magic Statics"** 特性。

1. 编译器在底层会为局部静态变量的初始化自动生成类似 `std::call_once` 的同步代码（通常利用 `__cxa_guard_acquire` 和 `__cxa_guard_release` 等系统内置函数）。
2. 同样地，在线程A构造 `single` 时，线程B访问这行代码会被挂起。
3. 标准同时保证了初始化完成后的**内存序**，即构造函数内的写操作 Happens-Before 其他线程获取到该静态变量的引用。
4. 因此，指令重排被编译器底层的同步原语严格约束，绝对不会出现“分配了内存返回了地址，但还没构造”的情况被其他线程看到。

### 总结

*   **双重检查锁定（DCLP）的缺陷**在于它试图在不阻塞线程的情况下实现高并发读，导致外层的 `if` 判断容易撞上“指令重排”后的半成品指针。
*   **`std::call_once` 和 静态局部变量（Meyers' Singleton）**之所以安全，是因为它们将同步的负担交给了编译器和C++标准库，底层通过**阻塞等待**和严格的**内存屏障**，确保了“先完全构造，再发布给其他线程”的绝对顺序，从根本上杜绝了指令重排的风险。

在现代C++编程中，**极力推荐使用 `Single2`（Meyers' Singleton）**，代码最少、最优雅，且由标准保证绝对线程安全。