## 浅谈悲观并发与乐观并发

在现代多核并发编程中，如何高效且安全地处理共享数据是核心难题。根据对并发冲突的预期不同，主要分为两种策略：**悲观并发控制**和**乐观并发控制**。

本文将详细解析这两种策略，并深入探讨无锁编程中的核心技术——**CAS（Compare-And-Swap）循环**，最后在 C++ 语境下对两者进行多维度的对比。

---

### 一、 悲观并发控制与加锁

#### 1. 核心思想
悲观并发控制（Pessimistic Concurrency Control）的态度是：“**冲突大概率会发生**”。因此，在访问共享资源之前，必须先获取锁，确保独占访问权。

#### 2. C++ 实现
在 C++ 中，通常使用 `std::mutex` 及其配套的 `std::lock_guard` 或 `std::unique_lock`。

```cpp
#include <mutex>

class PessimisticCounter {
private:
    int count_;
    std::mutex mtx_;
public:
    void increment() {
        // 加锁，阻塞其他试图访问的线程
        std::lock_guard<std::mutex> lock(mtx_); 
        count_++; 
        // 离开作用域自动解锁
    }

    int get() {
        std::lock_guard<std::mutex> lock(mtx_);
        return count_;
    }
};
```

#### 3. 特点
* **阻塞型**：如果一个线程持有锁，其他线程必须进入睡眠状态（操作系统级别的上下文切换）或在用户态自旋。
* **保证公平性与顺序性**：通过锁的机制，可以严格控制线程执行的先后顺序。

---

### 二、 乐观并发控制与无锁编程

#### 1. 核心思想
乐观并发控制的态度是：“**冲突大概率不会发生，即使发生也很容易解决**”。因此，线程不加锁，直接去修改共享数据。在提交修改前，检查数据是否被其他线程修改过。如果没被修改，则提交；如果被修改了，则重试。

#### 2. 基础：CAS（Compare-And-Swap）

CAS 是实现乐观并发的硬件级别原子指令。它包含三个参数：
1. **内存地址（V）**：要修改的变量的内存位置。
2. **期望值（E）**：认为该内存位置当前应该拥有的值。
3. **新值（N）**：想要写入的新值。

**CAS 逻辑**：“如果内存 V 中的值等于 E，则将其更新为 N，并返回 true；否则，什么都不做，并返回 false。” 这个操作是**原子的**（不可分割的）。

#### 3. CAS 循环
因为 CAS 可能会失败（其他线程抢先修改了数据），所以通常将 CAS 放在一个 `while` 循环中不断重试，直到成功为止。这就是所谓的 **CAS 循环** 或 **自旋**。

#### 4. C++ 实现
在 C++11 中，`std::atomic` 提供了 `compare_exchange_weak` 和 `compare_exchange_strong` 来实现 CAS。

```cpp
#include <atomic>

class OptimisticCounter {
private:
    std::atomic<int> count_;
public:
    OptimisticCounter() : count_(0) {}

    void increment() {
        int expected;
        int desired;
        bool success;
        
        // CAS 循环
        do {
            expected = count_.load(std::memory_order_relaxed); // 1. 读取当前值作为期望值
            desired = expected + 1;                             // 2. 在本地计算新值
            // 3. 尝试原子性地更新。如果 count_ 仍然是 expected，则更新为 desired 并返回 true
            //    如果 count_ 被其他线程改了，则将 expected 更新为 count_ 当前的真实值，并返回 false
            success = count_.compare_exchange_weak(expected, desired, 
                          std::memory_order_relaxed, std::memory_order_relaxed);
        } while (!success); // 4. 如果失败，则用新的 expected 自动进入下一轮循环
    }

    int get() {
        return count_.load(std::memory_order_relaxed);
    }
};
```

**`weak` vs `strong`**：
* `compare_exchange_weak` 有时会出现**伪失败**（Spurious Failure），即内存值明明等于期望值，却返回了 false。但因为外面有 `while` 循环，伪失败只是多循环一次而已。在循环场景下，使用 `weak` 性能通常比 `strong` 更好（少了保证强一致性的开销）。

---

### 三、 深入对比：乐观并发（CAS） vs 悲观并发（加锁）

| 维度           | 悲观并发控制（加锁 Mutex）                               | 乐观并发控制（CAS 循环）                                     |
| :------------- | :------------------------------------------------------- | :----------------------------------------------------------- |
| **核心假设**   | 冲突频繁发生                                             | 冲突很少发生                                                 |
| **机制**       | 预先加锁，互斥访问                                       | 先读后改，提交时验证                                         |
| **阻塞行为**   | **阻塞型**。线程拿不到锁会被挂起，发生上下文切换。       | **非阻塞型**。失败的线程会在 CPU 上自旋重试，不放弃 CPU 时间片。 |
| **适用场景**   | 临界区复杂、耗时较长、冲突率高。                         | 临界区极简（如计数器、入队出队）、冲突率低。                 |
| **高并发表现** | 线程增多时，吞吐量下降明显（锁竞争与上下文切换开销大）。 | 线程增多但冲突少时，吞吐量极高；**但冲突极高时，CPU 会因不断重试而飙升至 100%**。 |
| **死锁风险**   | 有死锁风险                                               | **无死锁风险**                                               |
| **优先级反转** | 可能发生（低优先级线程持有锁，高优先级线程被阻塞）       | 不会发生                                                     |
| **内存开销**   | 需要维护锁对象，且会陷入内核态。                         | 仅需普通变量，通常在用户态完成。                             |

---

### 四、 CAS 循环带来的经典问题与解决

虽然 CAS 实现了无锁编程，但它并非银弹，会带来两个经典问题：

#### 1. ABA 问题
CAS 只能检测值是否发生了变化，不能检测变化的历史。如果线程 1 读取了值 A，线程 2 将值改为 B，然后又改回 A。此时线程 1 执行 CAS，发现值还是 A，于是操作成功。但这在某些业务逻辑（如无锁栈的指针操作）中会导致严重错误。

**C++ 解决方案**：使用带有版本号的 CAS，或直接使用 C++20 引入的 `std::atomic<std::shared_ptr>`，或者自己封装一个包含 `{value, version}` 的结构体进行 CAS。

#### 2. 活锁与 CPU 暴增
当并发冲突极高时，多个线程可能同时不断失败并重试，导致 CPU 空转，甚至比加锁还要慢。这是一种“活锁”状态。

**C++ 解决方案**：
* 如果发现重试次数超过阈值，可以引入短暂的退避策略（Backoff，如 `std::this_thread::yield()` 让出 CPU）。
* 在极高冲突场景下，**退回到悲观加锁策略**往往是更好的选择。

---

### 五、 进阶：从 CAS 到复杂的无锁数据结构

单纯的 CAS 循环通常只适用于简单的状态原子更新（如计数器）。如果要在 C++ 中实现复杂的无锁数据结构（如无锁队列），需要解决更复杂的问题。

**问题**：假设要实现无锁栈的 `push` 操作，需要两步：
1. 创建新节点 `node`。
2. 将 `node->next` 指向当前的头节点 `head`。
3. 将 `head` 指向 `node`。

如果在第 2 步和第 3 步之间，`head` 被其他线程修改了，我们的 CAS 就会失败。此时需要重试。但重试时，`node->next` 已经被赋了旧值。

**C++ 标准做法**：
```cpp
void push(T value) {
    Node* newNode = new Node(std::move(value));
    Node* oldHead = head.load();
    newNode->next = oldHead;
    // 循环直到 head 成功指向 newNode
    while (!head.compare_exchange_weak(oldHead, newNode)) {
        // CAS 失败时，oldHead 会被自动更新为 head 的最新值
        // 我们只需要重新链接 newNode->next 即可，然后继续重试
        newNode->next = oldHead;
    }
}
```
*注意：上述无锁栈代码在内存回收（GC）方面存在严重问题（节点被弹出后，其他线程可能还在访问它），这就是为什么 C++20 引入了 `std::atomic<std::shared_ptr>` 来简化无锁数据结构的内存管理。*

### 六、 总结

1. **悲观并发（加锁）**：以空间和线程挂起换取安全性。逻辑直观，适合复杂、耗时的临界区。但在高并发下容易成为性能瓶颈。
2. **乐观并发（CAS）**：以 CPU 自旋重试换取非阻塞的高吞吐量。适合简单、快速的临界区，以及读多写少的场景。
3. **C++ 实践建议**：
   * 优先使用 `std::atomic` 和 CAS 处理简单的基础类型并发。
   * 不要盲目追求“无锁”。如果发现 CAS 循环在特定业务下冲突率极高，应果断改用 `std::mutex`。
   * 构建复杂的无锁数据结构极具挑战性（需处理内存序、ABA、内存回收等问题）。C++20 的 `std::atomic_ref` 和 `std::atomic<std::shared_ptr>` 是构建现代无锁结构的利器。





## 无锁并发的快照

**Q: 无锁并发操作共享数据时，用old_xxx缓存访问的数据，防止数据在其他线程被改变**

你的总结非常到位，但这句话中有一个词需要稍微纠正一下：**不是“防止”数据在其他线程被改变，而是“发现”数据是否被其他线程改变。**

这就是无锁编程中最核心的设计模式：**乐观并发控制**与 **CAS（Compare-And-Swap）循环**。

你提到的 `old_xxx`，在无锁编程中通常被称为**本地快照**。我们以代码中的 `release_ref` 函数为例来深入剖析这个机制：

```cpp
void release_ref()
{
    // 1. 拍快照：缓存当前共享数据的状态
    node_counter old_counter = count.load(std::memory_order_relaxed);
    node_counter new_counter;
    do
    {
        // 2. 基于快照在本地进行修改，不碰共享数据
        new_counter = old_counter;
        --new_counter.internal_count; 
    }
    // 3. 提交：比较并交换
    while (!count.compare_exchange_strong(
        old_counter, new_counter,
        std::memory_order_acquire, std::memory_order_relaxed));
    // ...
}
```

### 为什么必须用 `old_xxx` 缓存（拍快照）？

#### 1. 无法直接修改共享数据
在多核 CPU 中，你不能直接执行 `count.internal_count--`。因为这是一个“读-改-写”（RMW）操作，包含了三个步骤：读取内存到寄存器、寄存器减1、写回内存。
如果在写回之前，其他线程刚好修改了 `count`，你写回时就会覆盖其他线程的修改（数据竞争）。所以你必须先读取一个基础值 `old_counter`，在本地算出目标值 `new_counter`。

#### 2. 乐观地假设“没人动过”
拿着 `old_counter` 算出了 `new_counter` 后，你准备把 `new_counter` 写回内存。但在你计算这段时间里，**其他线程完全可能已经修改了 `count`**。
无锁编程的态度很“乐观”：它假设在这极短的时间内没有其他线程捣乱。于是它使用硬件级别的原子指令 `compare_exchange_strong` 去写回。

#### 3. 发现被改变，则重试（而不是阻止改变）
`compare_exchange_strong(old_counter, new_counter)` 底层执行的是这样的逻辑（由 CPU 硬件保证原子不可打断）：
* **比较**：内存中 `count` 当前的值 == `old_counter`？
* **如果是（没被别人改过）**：把 `new_counter` 写入内存。操作成功！
* **如果否（被别人改过了）**：**拒绝写入 `new_counter`**，并且**把内存中最新的值刷新到 `old_counter` 中**。返回 `false`。

这就是为什么你的总结需要从“防止”改为“发现”的原因：
**无锁操作根本无法阻止其他线程去写共享数据。它的做法是：如果我发现我动手期间，数据被别人动过了，我就放弃我刚才的计算结果，拿着别人更新后的最新数据（CAS失败时自动更新的 `old_xxx`），重新再算一遍（`do-while` 循环），直到我成功提交为止。**

### 对比：加锁机制 vs 无锁机制

* **加锁（悲观并发）**：操作前先锁住数据。**“我不允许你们任何人动它，直到我操作完。”** -> 这叫“防止”。
* **无锁（乐观并发）**：拿个快照（`old_xxx`）在本地算。**“你们谁都可以动它。但我提交时如果发现它和我的快照不一样了，我就重试。”** -> 这叫“发现并恢复”。

### CAS 失败时 `old_xxx` 的自动更新

注意 C++ 中 `compare_exchange` 的一个非常关键的设计：
```cpp
bool compare_exchange_strong(T& expected, T desired, ...);
```
`expected` 是**按引用传递**的。这意味着，当 CAS 失败时（说明内存值变了），硬件会自动把内存中的最新值塞回 `expected`（也就是你的 `old_xxx`）变量里。

所以代码里的 `do-while` 循环不需要在循环体开头重新调用 `load()`，因为 `compare_exchange_strong` 失败时，已经帮 `old_counter` 更新为最新状态了，下一轮循环直接基于最新的 `old_counter` 继续计算即可。

### 总结
你观察到的现象是正确的。在无锁栈和无锁队列的所有读写代码里，你都会看到大量这样的模式：先 `load()` 拿到 `old_xxx`，在本地算出 `new_xxx`，然后进入 `while(!compare_exchange(old_xxx, new_xxx))` 循环。这正是实现无锁并发安全的核心基石。



