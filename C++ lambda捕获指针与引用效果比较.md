# C++ lambda捕获指针与引用效果比较

## cpp lambda作伪闭包时，[]内捕获指针是否与捕获引用拥有相同效果？例如[this] , `[func](func为函数名)`

简短的回答是：**在底层机制和某些特定场景下，它们并不完全等价，但在“访问被捕获对象”的效果上，它们往往表现得非常相似。**

为了彻底搞清楚这个问题，我们需要区分“捕获指针本身”和“捕获引用”，并结合你给出的 `[this]` 和 `[func]` 两个具体场景来详细分析。

---

### 一、 指针捕获 vs 引用捕获的本质区别

假设有一个外部变量 `int a = 10;`，以及一个指向它的指针 `int* p = &a;`。

1. **捕获指针 `[p]`（按值捕获指针）：**
   ```cpp
   int* p = &a;
   auto lam1 = [p]() { return *p; };
   ```
   * **本质**：在 lambda 内部拷贝了一个新的指针变量 `p`。这个新指针指向 `a`。
   * **效果**：你可以通过解引用 `*p` 访问甚至修改 `a` 的值。但是，如果在 lambda 生成后，外部把 `p` 指向了别的地址（`p = &b;`），lambda 内部的 `p` 依然指向 `a`。

2. **捕获引用 `[&a]` 或 `[&p]`：**
   ```cpp
   int* p = &a;
   auto lam2 = [&a]() { return a; };   // 捕获 a 的引用
   auto lam3 = [&p]() { return *p; };  // 捕获 p 这个变量的引用
   ```
   * **本质**：在 lambda 内部存储的是一个引用（底层通常也是通过指针实现），它绑定到了外部的变量 `a` 或指针变量 `p` 上。
   * **效果**：如果是 `[&p]`，外部把 `p` 指向了 `b`，lambda 内部通过 `*p` 访问到的也会是 `b`。如果是 `[&a]`，则直接绑定 `a`，与指针无关。

**总结对比：**
* **相同点**：两者都可以用来间接修改外部的对象，并且都面临外部对象被销毁导致悬空（Dangling）的生命周期风险。
* **不同点**：捕获指针 `[p]` 拷贝了指针的**值**（地址）；捕获引用 `[&p]` 绑定了指针的**变量本身**。引用不能重新绑定，而按值捕获的指针在 lambda 内部可以指向其他地址（如果是非 const lambda）。

---

### 二、 分析场景 1：`[this]`

在 C++ 中，`this` 是一个指向当前对象实例的纯右值指针。

```cpp
class MyClass {
    int data = 42;
public:
    void makeLambda() {
        // [this] 捕获了当前对象的指针
        auto lam = [this]() { 
            return data; // 等同于 this->data
        };
    }
};
```

**`[this]` 与捕获引用效果一样吗？**
1. **在语法层面上，你不能写 `[&this]`**。因为 `this` 是一个纯右值，不能被取地址，也不能用非 const 引用去绑定它。所以 C++ 规定 `[this]` 就是**按值捕获** `this` 指针。
2. **在效果上，`[this]` 类似于捕获了对象本身的引用 `[&]`**：
   * 当你在 lambda 内部使用 `data` 时，实际上是 `this->data`，你可以直接读写当前对象的成员，这和通过引用访问对象成员的效果完全一致。
   * **生命周期风险相同**：如果当前对象被销毁了，lambda 内部的 `this` 就成了悬空指针，这与捕获引用导致的悬空引用是同样的未定义行为（UB）。
3. **C++20 的补充**：在 C++20 中引入了 `[&]` 会隐式捕获 `*this` 的特性（如果使用了 `[=]`，也会捕获 `this` 指针）。为了避免指针悬空问题，C++17 引入了 `[*this]`，它会把当前对象**拷贝**一份到 lambda 中，从而彻底切断生命周期依赖。

---

### 三、 分析场景 2：`[func]`（func 为函数名）

在 C++ 中，函数名（或全局函数指针）在绝大多数情况下会**退化为函数指针**。

```cpp
void func() { /* ... */ }

auto lam = [func]() {
    func(); 
};
```

**`[func]` 与捕获引用效果一样吗？**
1. **对于全局/静态函数：**
   `[func]` 实际上捕获的是一个函数指针（按值拷贝了一个地址）。全局/静态函数的地址在程序运行期间是固定不变的，永远不会失效。在这种情况下，按值捕获指针 `[func]` 和按引用捕获 `[&func]` **效果完全一致**，且都绝对安全。通常我们写 `[func]` 即可，没必要写 `[&func]`。
   
2. **对于局部函数对象（如 lambda 表达式或 std::function）：**
   如果 `func` 是一个局部变量：
   ```cpp
   void outer() {
       auto func = [](){ /*...*/ }; // 局部 lambda 变量
       auto lam1 = [func]() { func(); };  // 按值捕获（拷贝了一份 func 对象）
       auto lam2 = [&func]() { func(); }; // 按引用捕获
   }
   ```
   此时两者**效果完全不同**：
   * `[func]` 会把 `func` 这个对象拷贝一份到 lambda 内部，lambda 拥有独立的生命周期，即使外部的 `func` 被销毁了，lambda 依然可以安全调用。
   * `[&func]` 则只保存了外部 `func` 的引用，如果 `outer` 函数结束，`lam2` 调用时会导致崩溃。

---

### 总结

| 捕获方式                     | 底层机制                       | 效果对比                                             | 生命周期风险                                |
| :--------------------------- | :----------------------------- | :--------------------------------------------------- | :------------------------------------------ |
| **`[this]`**                 | 按值拷贝了对象指针             | 效果等同于“捕获了对象本身的引用”，可以直接读写成员。 | 对象销毁后，lambda 内的 `this` 悬空（UB）。 |
| **`[func]` (全局/静态函数)** | 函数名退化为指针，按值拷贝指针 | 效果等同于“引用捕获”，但更安全。                     | 无风险（地址固定）。                        |
| **`[func]` (局部函数对象)**  | 按值拷贝整个函数对象           | 效果**不同于**引用捕获，lambda 自带副本。            | 无风险。                                    |
| **`[&obj]` / `[&this]`**     | 保存外部变量的引用             | 操纵外部变量本体。                                   | 外部变量销毁后悬空（UB）。                  |

**结论**：
不能简单地说“捕获指针和捕获引用效果相同”。捕获指针本质上是**按值捕获了一个地址**，而捕获引用是**按引用绑定了一个变量**。只是当我们通过指针去操作它指向的对象时，表现出来的行为（能修改原对象、存在生命周期依赖）与捕获引用非常像。对于 `[this]` 和全局函数名，写 `[this]` 和 `[func]` 是最标准、最符合直觉的做法。



