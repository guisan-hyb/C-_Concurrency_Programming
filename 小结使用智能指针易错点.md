## 小结使用智能指针易错点

**Q: 为什么cpp智能指针传自定义删除器的方式不一样，有的在<>内，有的在()初始化的时候，为什么这么设计**

C++ 智能指针中，`std::unique_ptr` 和 `std::shared_ptr` 传递自定义删除器的方式确实不同，这背后的根本原因是**两者的设计目标和底层实现机制完全不同**。

简单来说：
*   `unique_ptr` 在**编译期**就要知道删除器的类型，为了**零开销抽象**（不增加额外内存和时间成本）。
*   `shared_ptr` 为了灵活性，使用了**类型擦除**技术，在**运行期**存储删除器，为了使得所有同类型的 `shared_ptr` 保持一致。

下面是详细的原因分析：

---

### 1. `std::unique_ptr`：为什么在 `<>` 模板参数里？

`std::unique_ptr` 的模板声明如下：
```cpp
template <typename T, typename Deleter = std::default_delete<T>>
class unique_ptr;
```

**设计原因：追求极致的性能（零开销）**

*   **删除器类型是智能指针类型的一部分**：
    因为删除器的类型 `Deleter` 是在模板参数列表 `<>` 中指定的，所以 `unique_ptr<int, MyDeleter>` 和 `unique_ptr<int, YourDeleter>` 是**两个完全不同的类型**。编译器在编译期就能确定所有的调用，没有虚函数开销。
*   **空基类优化**：
    如果你传入的删除器是一个无状态的仿函数或无捕获的 Lambda，它不占用任何内存。C++ 标准要求实现使用**空基类优化**。此时，`unique_ptr` 的大小就和裸指针一模一样（通常是 1 个指针的大小，8 字节）。
    如果把删除器放在构造函数 `()` 里，`unique_ptr` 就必须额外开辟空间去存储这个对象，导致体积变大（变成 2 个指针大小）。
*   **支持函数指针**：
    如果你在 `<>` 里传一个函数指针类型（如 `void(*)(int*)`），`unique_ptr` 内部会存这个指针，依然能在编译期绑定。

**使用示例：**
```cpp
// 1. 传入仿函数类型
struct MyFileDeleter {
    void operator()(FILE* fp) const { if (fp) fclose(fp); }
};
std::unique_ptr<FILE, MyFileDeleter> up1(fopen("test.txt", "r"));

// 2. 传入函数指针类型
std::unique_ptr<FILE, void(*)(FILE*)> up2(fopen("test.txt", "r"), [](FILE* fp) { if (fp) fclose(fp); });
// 注意：对于有捕获的 lambda，不能直接放进 <> 里，因为 lambda 类型是唯一的且无法显式写出，这时候可以利用 decltype 或直接传 std::function，但这违背了 unique_ptr 的极致性能初衷。
```

---

### 2. `std::shared_ptr`：为什么在 `()` 构造函数里？

`std::shared_ptr` 的模板声明如下：
```cpp
template <typename T>
class shared_ptr;
```
注意，它**没有**删除器的模板参数。

**设计原因：追求极致的灵活性（类型擦除）**

*   **类型一致性**：
    `shared_ptr` 经常被放入容器中，或者作为基类和派生类之间的转换（例如 `shared_ptr<Base> pb = make_shared<Derived>();`）。如果删除器也作为模板参数，那么 `shared_ptr<Base, Del1>` 就无法和 `shared_ptr<Base, Del2>` 放在同一个 `vector` 里。为了保持所有 `shared_ptr<T>` 都是同一种类型，删除器的类型必须被“擦除”掉。
*   **控制块机制**：
    `shared_ptr` 底层维护一个控制块，这个控制块是动态分配的。控制块里不仅存储了引用计数，还存储了自定义删除器和分配器。因为控制块是在堆上 `new` 出来的，所以删除器无论多大，都存在堆上，不会影响 `shared_ptr` 对象本身的大小（`shared_ptr` 对象本身始终是 2 个指针大小：指向数据，指向控制块）。
*   **运行期绑定**：
    删除器在构造函数 `()` 中传入，被存入控制块。当引用计数归零时，控制块会调用存储的删除器。这其实是通过类型擦除（类似 `std::function`）实现的，有一点点运行期开销，但带来了极大的灵活性。

**使用示例：**
```cpp
FILE* fp = fopen("test.txt", "r");

// 传给构造函数 ()，随便什么 lambda 都行，可以有捕获
std::shared_ptr<FILE> sp1(fp, [some_state](FILE* f) { 
    if (f) fclose(f); 
    std::cout << "Closed with state\n"; 
});

// 甚至可以和默认删除器的 shared_ptr 混用
std::shared_ptr<FILE> sp2 = sp1; // 没问题，类型完全一致
```

---

### 总结对比

| 特性                    | `std::unique_ptr<T, Deleter>`               | `std::shared_ptr<T>`                   |
| :---------------------- | :------------------------------------------ | :------------------------------------- |
| **删除器位置**          | 模板参数 `<>` 内                            | 构造函数 `()` 内                       |
| **删除器类型**          | 必须在编译期已知                            | 运行期类型擦除（类似 `std::function`） |
| **类型一致性**          | 删除器不同则类型不同                        | 只要 `T` 相同，类型就相同              |
| **对象内存大小**        | 取决于删除器（空基类优化可为 1 个指针大小） | 固定大小（通常是 2 个指针大小）        |
| **性能开销**            | 零开销（编译期决议，可能被完全内联）        | 略有开销（需要调用控制块中的函数指针） |
| **支持有捕获的 Lambda** | 不直接支持（无法写在 `<>` 中）              | 完美支持                               |

**C++ 的设计哲学在这里体现得淋漓尽致：**
`unique_ptr` 是底层性能工具，它把所有能在编译期决定的东西都暴露在类型系统里，让你不为用不到的功能付代价；`shared_ptr` 是上层通用工具，它牺牲了一点点性能和内存，换取了到处传递的便利性。





**Q: 当类的构造函数为私有，为什么用传统std::unique_ptr<T>()/std::shared_ptr<T>()就行**

这是一个非常经典的问题！核心原因在于：**到底是谁在代码里执行了 `new T()`（即调用了私有构造函数）**。

简单来说：
*   **传统构造 `std::unique_ptr<T>(new T())`**：`new T()` 是**你自己**写的代码。如果你把它放在类的静态工厂方法里，你就有权限访问私有构造函数。
*   **`std::make_unique<T>()`**：`new T()` 是写在 **C++ 标准库**的模板函数内部的。标准库不是你的类的“友元”，自然没有权限调用私有构造函数。

下面为你详细拆解：

### 1. 传统构造方式：为什么行得通？

当类的构造函数是私有时，外部无法直接 `new` 这个对象。通常的做法是提供一个静态工厂方法。

在这个静态方法中，`new T()` 是由**类自己的代码**执行的：

```cpp
class MyClass {
private:
    MyClass() {} // 构造函数私有

public:
    // 静态工厂方法
    static std::unique_ptr<MyClass> Create() {
        // 这里的 new MyClass() 是在 MyClass 的作用域内执行的
        // 静态成员函数有权访问私有构造函数！
        return std::unique_ptr<MyClass>(new MyClass()); 
    }
};
```
在这个例子中，`std::unique_ptr` 的构造函数只接收了一个 `MyClass*` 指针。**智能指针本身并没有尝试去调用 `MyClass()`，它只是接管了你给它的指针**。创建对象的动作是由你的 `Create` 函数完成的，所以不违反封装原则。

### 2. `std::make_unique` / `std::make_shared`：为什么行不通？

如果你试图用 `std::make_unique` 来简化代码，会直接编译报错：

```cpp
class MyClass {
private:
    MyClass() {} 

public:
    static std::unique_ptr<MyClass> Create() {
        // 编译报错！error: calling a private constructor
        return std::make_unique<MyClass>(); 
    }
};
```

**为什么报错？**

如果你去看 C++ 标准库中 `std::make_unique` 的源码（伪代码），它大概是这样的：

```cpp
namespace std {
    template<typename T, typename... Args>
    unique_ptr<T> make_unique(Args&&... args) {
        // 注意这里：new T(std::forward<Args>(args)...) 
        // 这行代码是在 std 命名空间里执行的！
        return unique_ptr<T>(new T(std::forward<Args>(args)...));
    }
}
```

当编译器实例化 `std::make_unique<MyClass>()` 时，它会在标准库的作用域里尝试执行 `new MyClass()`。因为 `std::make_unique` 既不是 `MyClass` 的成员函数，也不是它的友元，所以它**没有权限**访问 `MyClass` 的私有构造函数，导致编译失败。

---

### 补充：现代 C++ 如何既用 `make_unique` 又保持构造私有？（Passkey Idiom）

虽然直接用 `std::unique_ptr<T>(new T())` 行得通，但现代 C++ 通常推荐尽量使用 `std::make_unique`（为了避免内存泄漏、减少一次内存分配等）。

为了解决“私有构造函数”与“`make_unique` 无法访问”的矛盾，C++ 社区发明了 **Passkey Idiom（密钥模式 / 令牌模式）**。

思路是：把构造函数设为公开，但要求传入一个特殊的“令牌”类，而这个令牌类的构造函数是私有的，且只把特定的工厂函数设为它的友元。

```cpp
class MyClass {
public:
    // 定义一个私有的令牌
    class Token {
    private:
        Token() {}
        friend std::unique_ptr<MyClass> CreateMyClass();
    };

    // 构造函数公开，但必须传入 Token 对象
    // 由于 Token 无法被外部构造，所以外部依然无法直接 new MyClass
    explicit MyClass(Token) {} 

private:
    // 其他私有成员...
};

// 只有这个全局函数能创建 Token，进而创建 MyClass
std::unique_ptr<MyClass> CreateMyClass() {
    // make_unique 内部执行 new MyClass(MyClass::Token())
    // 此时 Token() 是在这个友元函数里调用的，合法！
    // MyClass(Token) 是公开的，合法！
    return std::make_unique<MyClass>(MyClass::Token());
}
```

### 总结

*   `std::unique_ptr<T>(new T())` 能行，是因为对象的创建（`new T()`）发生在**你有权限的代码域**里，智能指针只是个接盘侠。
*   `std::make_unique<T>()` 不行，是因为对象的创建发生在**标准库内部**，标准库没有权限碰你的私有构造函数。
*   这是 C++ 访问控制机制和模板实例化机制的必然结果，体现了 C++ 对封装性的严格保护。





**Q: `std::shared_ptr`返回空值时，可以返回`nullptr`，也可以返回`std::shared_ptr<T>()`，但为什么不能返回`std::make_shared<T>`() 呢**

这是一个非常好的问题，触及了 `std::shared_ptr` 的核心设计理念。

简单来说：**因为 `std::make_shared<T>()` 的作用是“在堆上创建一个新对象并交给智能指针管理”，它返回的永远是一个指向有效对象的非空指针。**

下面详细对比这三者的区别：

### 1. `return nullptr;` （合法）

在 C++11 起，`std::shared_ptr` 提供了一个接收 `std::nullptr_t` 的隐式构造函数。
当你返回 `nullptr` 时，编译器会隐式地调用这个构造函数，生成一个**空的** `shared_ptr`（不持有任何对象，引用计数为0）。

### 2. `return std::shared_ptr<T>();` （合法）
这是调用 `shared_ptr` 的**默认构造函数**。它的效果和返回 `nullptr` 完全一样，也是生成一个空的 `shared_ptr`。这种写法在 C++11 之前（还没有隐式 `nullptr` 构造的时代）是标准做法。

### 3. `return std::make_shared<T>();` （违背初衷）
`std::make_shared<T>()` 的底层做了两件事：
1. 在堆上分配一块内存（足以容纳控制块和 `T` 类型的对象）。
2. 在这块内存上调用 `T` 的默认构造函数，原地构造出一个 `T` 对象。
3. 返回一个指向这个新创建对象的 `shared_ptr`。

**所以，`std::make_shared<T>()` 返回的指针永远不会是空的！**

如果你在 `pop()` 函数中这样写：
```cpp
if (!old_head) {
    return std::make_shared<T>(); // 错误的做法
}
```
会发生什么？
1. **内存分配**：哪怕栈是空的，它也会在堆上强行 `new` 出一个 `T` 类型的对象（比如 `int(0)`）。
2. **语义错误**：调用 `pop()` 的人拿到这个指针后，检查 `if (ptr == nullptr)` 会发现是 `false`。调用方会误以为**栈没空，成功弹出了一个数据**，而这个数据其实是个凭空捏造出来的默认值（比如 0）。这在逻辑上是完全不可接受的。

### 总结

*   **`nullptr` / `std::shared_ptr<T>()`**：我要一个“空壳”智能指针，里面什么都不装，不分配堆内存。用来表示“没有数据”。
*   **`std::make_shared<T>()`**：我要在堆上真刀真枪地构造一个 `T` 类型的对象，并把它装进智能指针里。用来表示“这是一个新创建的数据”。

因此，当你需要表示“空”、“没找到”、“栈为空”这种无数据的语义时，只能用前两者，绝不能用 `make_shared`。





