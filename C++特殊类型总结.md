# C++特殊类型总结

## C++只能移动、不能拷贝类型总结

在C++11引入移动语义后，标准库中出现了大量**只能移动、不能拷贝**的类型。这些类型的设计往往遵循一个核心原则：**唯一所有权或唯一语义**。

如果允许拷贝，就会导致多个对象持有同一个底层资源，从而引发重复释放、数据竞争或逻辑混乱。

以下是C++中常见的只能移动类型总结，按其应用场景分类：

---

### 一、 内存与资源管理类（唯一所有权）

这类类型代表“唯一拥有某个资源”，转移所有权意味着原对象必须失去控制。

#### 1. `std::unique_ptr`
*   **为什么不能拷贝：** 它独占所指向对象的内存。如果允许拷贝，就会有两个 `unique_ptr` 指向同一块内存，析构时会导致 `double free`。
*   **移动语义：** 移动后，原 `unique_ptr` 变为 `nullptr`，所有权完全转移给新对象。
*   *(对比：`std::shared_ptr` 允许拷贝，因为它是共享所有权，内部有引用计数)*

#### 2. `std::unique_lock`
*   **为什么不能拷贝：** 它代表对互斥锁的独占所有权。如果允许拷贝，两个对象都认为自己持有锁，会导致解锁逻辑混乱。
*   **移动语义：** 允许将锁的所有权从一个作用域或对象转移到另一个。
*   *(对比：`std::lock_guard` 不仅不能拷贝，也不能移动，它的生命周期严格绑定作用域)*

---

### 二、 并发与线程类（底层系统资源）

操作系统级别的线程和同步原语，本质上是不可复制的。

#### 1. `std::thread`
*   **为什么不能拷贝：** 操作系统线程是单一实体。如果允许拷贝，两个C++对象会管理同一个OS线程，当它们析构时，系统不知道该听谁的（join 还是 detach）。
*   **移动语义：** 允许将线程的控制权（句柄）从一个 `std::thread` 对象转移给另一个。转移后，原对象不再代表任何线程（`joinable() == false`）。

#### 2. `std::promise`
*   **为什么不能拷贝：** 如前所述，它代表一次性的“设置值”的承诺。如果允许拷贝，两个对象都可以向同一个共享状态设置值，违反 `set_value` 只能调用一次的语义，会抛出 `std::future_error`。
*   **移动语义：** 允许将设置值的承诺转移给另一个对象（通常是转移给子线程）。

#### 3. `std::future`
*   **为什么不能拷贝：** 它代表获取异步结果的唯一凭证。如果允许拷贝，多个对象调用 `get()` 会导致数据竞争，或者因 `get()` 只能调用一次而引发异常。
*   **移动语义：** 转移获取结果的权利。
*   *(对比：`std::shared_future` 允许拷贝，专供多线程同时读取结果)*

#### 4. `std::packaged_task`
*   **为什么不能拷贝：** 它包装了一个可调用对象，并绑定了一个 promise。可调用对象可能包含不可拷贝的捕获列表，且内部的 promise 不可拷贝。
*   **移动语义：** 允许将打包的任务转移给其他线程执行。

#### 5. `std::stop_source` / `std::stop_token` (C++20)
*   **为什么不能拷贝：** `stop_source` 代表请求停止的权力，虽然内部有引用计数，但标准规定它是只移动的，以确保停止请求的发出者具有唯一性控制权。

---

### 三、 I/O 与文件系统类（系统句柄）

操作系统对文件句柄、网络套接字等资源也是唯一管理的。

#### 1. `std::fstream` / `std::ifstream` / `std::ofstream`
*   **为什么不能拷贝：** 它们封装了底层的文件描述符（File Descriptor）。如果两个流对象拷贝共享同一个描述符，一个关闭了文件，另一个就会操作失效的句柄。
*   **移动语义：** C++11 赋予了它们移动构造和移动赋值的能力，允许将文件流的所有权从一个对象转移到另一个（例如将流放入容器中）。

#### 2. `std::filesystem::directory_iterator` / `recursive_directory_iterator`
*   **为什么不能拷贝：** 它们内部持有读取目录的系统状态，拷贝会导致迭代状态混乱。
*   **移动语义：** 允许转移迭代器位置。

---

### 四、 其他工具类

#### 1. `std::random_device`
*   **为什么不能拷贝：** 它通常封装了操作系统的真随机数生成器（如 `/dev/urandom`），这涉及到系统资源的占用，拷贝没有意义或被标准禁止。
*   **移动语义：** 允许转移。

---

### 💡 重要区分：只能移动 vs 不可移动且不可拷贝

在学习时，一定要区分“只能移动”和“不可移动且不可拷贝”这两种概念：

*   **只能移动：** 拷贝构造被删除，但**移动构造存在**。它们可以转移所有权（如 `unique_ptr`, `thread`, `promise`）。
*   **不可移动且不可拷贝：** 拷贝和移动构造都被删除。这类对象**只能原地使用**，或者通过引用传递。常见的有：
    *   `std::mutex`（互斥锁，状态不允许转移）
    *   `std::lock_guard`（严格绑定作用域）
    *   `std::atomic`（原子操作，拷贝和移动都破坏原子性）

### 总结：如何判断一个类型是否只能移动？

当你看到一个类型，思考以下两个问题：
1.  **如果我把这个对象复制一份，两个对象同时操作底层的资源，会出问题吗？**（比如双重释放、双重写入、双重 join）。
2.  **这个对象代表的是否是一种‘一次性’或‘排他性’的权力？**（比如只能获取一次结果，只能设置一次值）。

如果答案是“是”，那么C++标准库通常会将其设计为**只能移动**的类型。





## C++不可移动且不可拷贝类型总结

在C++中，**不可移动且不可拷贝**的类型是最严格的对象限制。这类对象被死死地“钉”在它被创建的内存位置上，既不能复制一份，也不能转移所有权。

这种限制通常源于两个原因：
1. **底层系统资源不可转移**：操作系统层面的句柄或状态本身就不允许移动。
2. **设计哲学上的严格约束**：强行限制对象的生命周期和作用域，确保逻辑的绝对安全。

以下是C++标准库中不可移动且不可拷贝的类型总结，以及它们背后的设计逻辑：

---

### 一、 同步原语（系统级互斥与等待）

操作系统的锁和条件变量，一旦初始化，其内存地址和底层状态就固定了。

#### 1. `std::mutex` / `std::recursive_mutex` / `std::shared_mutex` 等
*   **为什么不可拷贝：** 拷贝一个锁毫无逻辑意义。如果对象A持有锁，把A拷贝给B，B也应该持有锁吗？这会引发死锁或逻辑混乱。
*   **为什么不可移动：** 这是更关键的一点。线程在等待锁时，实际上是记录了该锁对象所在的**内存地址**。如果允许移动，锁被挪到了新地址，正在等待的线程就会去检查旧地址，导致永久阻塞或崩溃。POSIX 线程和 Windows API 中的互斥锁都不支持移动。

#### 2. `std::condition_variable` / `std::condition_variable_any`
*   **为什么不可移动且不可拷贝：** 条件变量必须与特定的互斥锁配合使用，且内部维护着等待队列。移动它会导致正在等待的线程失去联系，破坏并发语义。

#### 3. `std::semaphore` (C++20) / `std::latch` (C++20) / `std::barrier` (C++20)
*   **原因同上：** 这些都是系统级的同步机制，其内部状态与特定内存地址绑定，移动会导致未定义行为。

---

### 二、 严格的作用域守护（RAII 绑定）

这类类型的设计初衷就是“生与死都在这个作用域内”，移动它们会违背其设计初衷。

#### 1. `std::lock_guard`
*   **为什么不可拷贝：** 它独占锁的所有权。如果允许拷贝，两个 `lock_guard` 都认为自己持有锁，析构时会解锁两次。
*   **为什么不可移动：** 它的设计哲学就是**严格的作用域绑定**。你只能在进入作用域时构造它，离开作用域时自动析构释放锁。如果允许移动，你就可以把锁“偷”出去，这破坏了 RAII 的确定性，容易引发死锁。
*   *(对比：`std::unique_lock` 是可移动的，因为它被设计用于更复杂的场景，如条件变量等待时需要移交锁的所有权)*

---

### 三、 原子类型

#### 1. `std::atomic<T>`
*   **为什么不可拷贝：** 拷贝操作包含两个步骤（读取源、写入目标），这**不是原子的**。如果允许拷贝，`auto b = a;` 就会破坏原子性的保证，在拷贝的过程中其他线程可能修改了 `a`。
*   **为什么不可移动：** 
    1. 同样，移动操作也不是原子的。
    2. 许多硬件架构（如 x86）对原子变量的地址有严格要求。原子操作通常依赖特定的 CPU 指令（如 `lock cmpxchg`），这些指令作用于特定的内存地址。如果对象被移动到了新地址，原本的原子保证可能失效。
*   **如何传递：** 必须通过引用传递，或者通过 `load()` 和 `store()` 进行底层数值的读写。

---

### 四、 其他系统相关

#### 1. `std::type_info`
*   **为什么不可移动且不可拷贝：** 它是 `typeid` 运算符返回的结果，由编译器在编译期生成。它代表类型的元数据，存储在只读的数据段中。程序没有权限去拷贝或移动编译器生成的内部结构。

---

### 💡 如何使用不可移动且不可拷贝的对象？

由于这类对象既不能按值传递，也不能按值返回，也不能放入 `std::vector` 等容器中，它们的使用方式非常受限：

1.  **只能作为局部变量**：在函数栈上创建，函数结束时自动销毁。
    ```cpp
    void func() {
        std::mutex mtx; // 局部互斥锁
        std::lock_guard<std::mutex> lock(mtx); // 局部守护
    } // 自动解锁
    ```
2.  **作为类的成员变量**：如果类包含不可移动/不可拷贝的成员，那么该类本身默认也会变成不可移动/不可拷贝的，除非你显式定义移动构造函数并重新初始化这些成员（例如重新初始化一个 mutex，这通常是不推荐的）。
3.  **必须通过引用传递**：
    ```cpp
    void wait_for_signal(std::condition_variable& cv, std::mutex& mtx); // 必须传引用
    ```
4.  **通过指针间接使用**：如果非要动态分配，必须使用 `std::unique_ptr`，但更推荐直接在稳定的作用域中创建对象。

### 💻 如何自己实现一个不可移动且不可拷贝的类？

在 C++11 之后，标准做法是显式删除这些函数：

```cpp
class NonMovableNonCopyable {
public:
    // 删除拷贝操作
    NonMovableNonCopyable(const NonMovableNonCopyable&) = delete;
    NonMovableNonCopyable& operator=(const NonMovableNonCopyable&) = delete;

    // 删除移动操作
    NonMovableNonCopyable(NonMovableNonCopyable&&) = delete;
    NonMovableNonCopyable& operator=(NonMovableNonCopyable&&) = delete;

    // 保留默认构造和析构
    NonMovableNonCopyable() = default;
    ~NonMovableNonCopyable() = default;
};
```

### 总结对比

| 类型特性             | 拷贝构造 | 移动构造 | 典型代表                         | 设计目的                         |
| :------------------- | :------- | :------- | :------------------------------- | :------------------------------- |
| **可拷贝可移动**     | 允许     | 允许     | `std::string`, `std::vector`     | 普通值类型，最灵活               |
| **只能移动**         | 删除     | 允许     | `std::unique_ptr`, `std::thread` | 独占所有权，转移资源             |
| **不可移动不可拷贝** | 删除     | 删除     | `std::mutex`, `std::atomic`      | 系统级绑定/严格作用域/原子性保护 |